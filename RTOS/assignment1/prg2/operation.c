#include "header.h"
#include<stdio.h>

int sum(int num, int arr_sum){
        return num + arr_sum;
}

float average(int arr_sum, float count){
        return (arr_sum/count);
}

int product(int num, int arr_prod){
        return num * arr_prod;
}

